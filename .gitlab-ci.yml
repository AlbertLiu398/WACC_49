stages:
  - build
  - test

build:
  stage: build
  script:
    - make clean 
    - scala-cli compile .
  tags:
    - shell

test:
  stage: test
  script:
  echo "Testing the project"
  #  - scala-cli test .
  tags:
    - shell


